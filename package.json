{
  "name": "argus-mcp-server",
  "version": "1.4.0",
  "description": "Skopaq E2E Testing Agent - MCP Server for AI IDEs (Claude Code, Cursor, Windsurf, VS Code)",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types"
  },
  "dependencies": {
    "@cloudflare/workers-types": "^4.20250109.0",
    "@sentry/cloudflare": "^10.36.0",
    "agents": "^0.0.92",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "wrangler": "^4.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
